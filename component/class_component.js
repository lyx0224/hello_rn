import React, { Component } from 'react';
import { Text } from 'react-native'
//自定义componet(class组件)
export default class MyClassComponent extends Component {
    render() {
        return (
            <Text> 我是class Component</Text>
        );
    }
}